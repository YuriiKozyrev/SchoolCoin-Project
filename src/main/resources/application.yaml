spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/test_schoolcoin_db
    username: postgres
    password: 1234
  flyway:
    baseline-on-migrate: true
